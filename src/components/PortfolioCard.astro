---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';

interface Props {
  piece: CollectionEntry<'portfolio'>;
}

const { piece } = Astro.props;
---

<a
  href={`/${piece.id}`}
  class="block h-full overflow-hidden group relative no-underline hover:no-underline"
  aria-label={piece.data.title}
>
  <Image
    src={piece.data.heroImage}
    alt={piece.data.alt}
    width=600
    class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
    style={piece.data.objectPosition ? `object-position: ${piece.data.objectPosition}` : undefined}
  />
  <div
    class="absolute inset-0 bg-linear-to-t from-bg/90 via-bg/40 to-transparent transition-opacity duration-300 flex items-end p-4 opacity-0 group-hover:opacity-100"
  >
    <div>
      <p class="text-text font-heading text-sm uppercase tracking-wider">{piece.data.title}</p>
      {
        (piece.data.medium || piece.data.dimensions) && (
          <p class="text-text-secondary text-xs mt-1">
            {[piece.data.medium, piece.data.dimensions].filter(Boolean).join(' — ')}
          </p>
        )
      }
    </div>
  </div>
</a>
